// Header-only ThreadPool; this TU exists so the CMake source list has a
// concrete file when INKLING_PARALLEL is ON. No definitions live here yet
// — kept around as a hook for future profiling/tracing helpers.
#include "thread_pool.h"
